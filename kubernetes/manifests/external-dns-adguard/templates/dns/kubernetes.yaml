apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: kubernetes
spec:
  endpoints:
  - dnsName: kubernetes.nullservers.com
    recordTTL: 300
    recordType: A
    targets:
    - 192.168.50.100
