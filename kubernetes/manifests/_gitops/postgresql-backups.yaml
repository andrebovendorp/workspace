apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: postgresql-backup
  namespace: flux-cd
spec:
  interval: 1h
  path: ./kubernetes/manifests/postgresql-backup
  prune: true
  sourceRef:
    kind: GitRepository
    name: workspace
    namespace: flux-cd
  targetNamespace: postgresql-backup
