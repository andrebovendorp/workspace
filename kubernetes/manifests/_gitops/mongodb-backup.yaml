apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: mongodb-backup
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/manifests/mongodb-backup
  prune: true
  sourceRef:
    kind: GitRepository
    name: workspace
    namespace: flux-system
  targetNamespace: mongodb-backup
