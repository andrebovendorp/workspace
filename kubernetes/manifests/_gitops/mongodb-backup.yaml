apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: mongodb-backup
spec:
  interval: 5m0s
  path: ./kubernetes/manifests/mongodb-backup
  prune: true
  sourceRef:
    kind: GitRepository
    name: workspace
  targetNamespace: mongodb-backup # Change this to the namespace where you want to deploy the app
