apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: duplicati
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/manifests/duplicati
  prune: true
  sourceRef:
    kind: GitRepository
    name: workspace
  targetNamespace: duplicati
