apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: dns-updater
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/manifests/dns-updater
  prune: true
  sourceRef:
    kind: GitRepository
    name: workspace
    namespace: flux-system
  targetNamespace: dns-updater
