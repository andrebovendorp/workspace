apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-dns-adguard
  namespace: external-dns-adguard
spec:
  interval: 1h
  chart:
    spec:
      chart: kubernetes/manifests/external-dns-adguard
      sourceRef:
        kind: GitRepository
        name: workspace
        namespace: flux-system
      version: ">=0.1.0"

  values:
