apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: grav
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/manifests/grav
  prune: true
  sourceRef:
    kind: GitRepository
    name: workspace
  targetNamespace: grav
