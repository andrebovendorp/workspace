apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: adguard-sync
  namespace: fluxcd
spec:
  interval: 1h
  path: ./kubernetes/manifests/adguard-sync
  prune: true
  sourceRef:
    kind: GitRepository
    name: workspace
    namespace: fluxcd
  targetNamespace: adguard-sync
