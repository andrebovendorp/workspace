apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plex-application
  namespace: argocd
spec:
  project: platform
  source:
    repoURL: https://github.com/boveloco/workspace.git
    targetRevision: main
    path: kubernetes/manifests/plex
  destination:
    server: https://kubernetes.default.svc
    namespace: plex
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
