apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - service.yaml
  - deployment.yaml
  - cnp.yaml
  - glance.yaml
  - dockerhub-token.yaml
  - http-route.yaml
  - referencegrant.yaml

commonLabels:
  app: glance
  component: glance-app

namespace: glance
