apiVersion: v1
kind: Service
metadata:
  name: adventurelog-frontend
  namespace: adventurelog
spec:
  selector:
    app: adventurelog-frontend
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
