apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: {{ .Release.Name }}-cadvisor-dashboard
spec:
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 14282
  datasources:
  - inputName: DS_PROMETHEUS
    datasourceName: prometheus
