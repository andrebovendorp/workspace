apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: {{ .Release.Name }}-postgresql-dashboard
spec:
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 9628
  datasources:
  - inputName: DS_PROMETHEUS
    datasourceName: prometheus
