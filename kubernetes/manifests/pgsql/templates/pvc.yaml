apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgsql-data
  namespace: pgsql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi # Specify the desired storage size (e.g., 1Gi, 10Gi, 100Gi)
  storageClassName: ssd-storage-persistent
